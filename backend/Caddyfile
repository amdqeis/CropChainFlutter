{$DOMAIN} {
    encode zstd gzip
    reverse_proxy api:8000
    log {
        output file /data/access.log {
            roll_size 20MiB
            roll_keep 10
        }
        format json
    }
}
